# This service is now ONLY the ML risk classifier (scikit-learn/XGBoost, no DB, no Groq —
# everything else moved to node-api/Node.js). See backend/CLAUDE.md for why.

# Must match INTERNAL_API_KEY in node-api/.env — rejects any caller that isn't node-api.
INTERNAL_API_KEY="change-me-shared-secret-between-node-and-python"

PORT=8000
